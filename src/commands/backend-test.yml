description: >
    Backend tests for the gradle build
# parameters:
#     project:
#       type: string
#     buildSubdir:
#       type: string
#     test_name:
#       type: string
steps:
    - run:
        name: Shallow Checkout
        command: << include(scripts/shallow-checkout.sh) >>
    - attach_workspace:
        at: /tmp/workspace
    - run:
        name: setup_gradle_ci_init
        command: << include(scripts/initialize-gradle-dir.sh) >>
